#include <stdio.h>

int main()
{
        char *escapes = "";                                         // Initialize an empty string using a character pointer
        char escape[] = {'\a', '\b', '\f', '\n', '\r', '\t', '\v'}; // Initialize an empty string using a character array
}